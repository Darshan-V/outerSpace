import React from 'react'
import PlayPause from './PlayPause'

const Songbar = ({
  song,
  i,
  artistId,
  isPlaying,
  activeSong,
  handlePauseClick,
  handlePlayClick,
}) => {
  return <div></div>
}

export default Songbar
